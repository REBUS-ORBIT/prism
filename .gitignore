# --- editors / OS ---------------------------------------------------------
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# --- secrets --------------------------------------------------------------
.env
.env.*
!.env.example
!infra/.env.example
*.pem
*.key
*.pfx

# --- node / typescript ----------------------------------------------------
node_modules/
dist/
build/
*.tsbuildinfo
.eslintcache
.vite/
.nuxt/
.output/
coverage/
.turbo/
.pnpm-store/

# --- python (in case any tool drops files) --------------------------------
__pycache__/
*.pyc
.venv/
venv/

# --- .NET / Rhino / Visual Studio -----------------------------------------
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
TestResults/
publish/

# --- local data / scratch -------------------------------------------------
data/
tmp/
.tmp/
*.log
*.db
*.db-journal
*.sqlite
*.sqlite-journal
local-uploads/
# Per-job admin log exports pulled to disk during local debugging
job_*_logs.csv

# --- generated codegen outputs (they are produced from shared/contracts) --
shared/generated/

# --- hotpatch tooling cache (per-host file hashes, see tools/hotpatch-pc02.ps1) -
tools/.cache/

# --- agent release artifacts -----------------------------------------------
# Staging directory + zipped MSI bundles produced by build-agent-release.ps1
# (see PRISM/agent/install/).  Never commit these -- the canonical artifacts
# are the GitHub Releases assets created by the agent CI workflow.
agent/PRISM.Agent-v*.zip
agent/stage-v*/
